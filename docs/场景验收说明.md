# 聚鼎场景还原修正版

## 纠正范围
上一版整张房间图加本地摆放已撤下。备份 index.scenes-draft.html 仅用于回溯。
现版横竖两款分别接入聚鼎自身的场景01、02，共4个真实场景模板，另保留2款已确认的白底。
横版：浅色客厅、深色客厅。竖版：蓝色沙发客厅、米色休闲椅。

## 一手依据
scenes/juding-metadata.json 与 scenes/juding-portrait-metadata.json 为当前浏览器已请求的模板参数。
scenes/config-original.json 为当前接入的4个模板参数；scene-original.js 为可维护场景实现，index.html 内联同一实现。
使用 PSD 的 X/Y、PPC 裁切轮廓、M 变换矩阵、OS 原尺寸；素材按矩阵逆映射，并按 Multiply 正片叠底叠加于原材质及投影层。
页面实际 Raster 对象使用半尺寸 PNG、scaleX/scaleY=2，已按该行为绘制。
与之前不同，输出恢复聚鼎的1000×1000裁切构图，并非房间源图片自然比例。
白底渲染函数与原有模板不变；场景不再采用白底轮廓或人工加阴影。

## 同图验收
acceptance-scenes.html 左侧为聚鼎大图预览截图，右侧为本地导出。
compare-source.png 为两边同一素材；竖版以居中铺满方式裁切。测试只临时调用网页预览渲染，未保存设计或加入购物车。已恢复原横版场景01空白预览。
comparison-metrics.json 记录600×600对照的RGB平均绝对差，约1.3–2.0/255。这是全图统计，背景占比较大，不等于边缘全部准确，更不代表逐像素相同；仍需用户视觉确认。

## 功能测试
original-params-tests.json：四组尺寸/坐标/PNG测试与两组白底切回一致通过。
初轮两个PNG字节相等检查未通过；继续定位到Canvas读回前后采样变化（最大通道差3），未将这两个断言算作通过。
export-reset-tests.json：重新按像素复查四组旋转复位最大差0，四组实际下载处理函数返回有效PNG，8项通过。
实际下载测试截获链接检查PNG内容，避免向下载目录添加无用测试文件。

## 限制
场景资源是网站预览PNG，不声称高清生产素材。浏览器WebGL和本地Canvas采样、截图缩放及素材裁切比例666/1000与2/3有微小差异。尚未接入各产品场景03–06。

## 入口与回滚
工作台 http://127.0.0.1:8766/ ，图册 http://127.0.0.1:8766/acceptance-scenes.html 。
index.before-scenes.html 可回到仅白底版。build-v5.py 仅写历史白底副本，不覆盖现版入口。


## 全场景扩展
已新增横版及竖版各自的场景03–06，共8个；现有12个聚鼎原场景，加2个保留白底，共14个选项。
原始参数完整配置 scenes/config-all.json；本轮16个图层资源来源 scenes/remaining-downloads.json。
全部14个选项已逐个验证1000×1000画布、资源加载和实际PNG下载处理函数，结果 scenes/all-tests.json 全部通过。新增场景已生成图册并目视检查。
新增8个场景使用此前取得的各产品原始模板参数，尚未逐个制作网站截图并排比对，不宣称逐像素完全一致。
完整图册 all-scenes.html；扩展前备份 index.before-all-scenes.html。

## 样式分组补漏
当前网页核实每款有另一组：横版3：2无（b6bc6282d22059db91b1c3cdb32e4f3b），竖版2：3无（6e29807f69c9c22ca86bbbdcb6e8e7ff），每组当前均返回1个模板。此前所谓全部仅指第一组，已纠正。新增样式分组控件，7+1，每款8个，共16个展示项。加载及两组切换、PNG编码检查通过，见scenes/style-group-tests.json。新模板按原始Raster、PPC、M合成，未追加侧边。原7模板保持不变。
