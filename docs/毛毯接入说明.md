# 毛毯模板接入与验证

- JJ0519910：280克法兰绒毛毯（竖版）（聚鼎），产品 ID 101168，原始输入面677×1000，8模板。
- JJ0519911：310克法兰绒毛毯（横版）（聚鼎），产品 ID 81892，原始输入面1000×870，8模板。
- scenes/blanketVertical-source.json、blanketHorizontal-source.json 保存原站数据；对应config文件含本地分层资源路径。
- 使用既有曲面网格渲染器，原始16控制点与M变换、PPC裁切、分组顺序、材质图层；本轮未修改渲染器。
- 两款主图同素材对照原站截图；全部16结果已逐一视觉检查。其余14图尚未逐个截取原站对照。
- scenes/blanket-tests.json 共24项通过：上传处理函数1、16导出、两款裁切旋转重置、旧5产品主图回归。导出读取真实导出处理函数产生的PNG Blob，未向下载目录写入测试文件。
- 当前仅接入每款当前样式组的8模板；竖版原站还列出第二输入层（1000×1000），本次没有宣称接入该层对应的其他样式或尺寸变体。
- 部分模板使用局部放大，素材可能只显示一部分；生产尺寸文字保留原图，不是可选尺寸承诺。输出1000×1000预览，不是印刷文件。
- 原站仅在临时页面使用测试素材预览，无保存或购物车提交。
- 回滚：index.before-blankets.html 覆盖 index.html，已添加的素材与验收文件可留存。
