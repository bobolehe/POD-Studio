const {build}=require('./model.cjs'),fs=require('fs'),path=require('path'),assert=require('assert/strict');const source=JSON.parse(fs.readFileSync(path.join(__dirname,'sample-input.json'),'utf8'));
for(const market of ['UK','CA']){const p=build({...source,market});const dir=path.join(__dirname,'outputs','market-test-'+market);fs.mkdirSync(dir,{recursive:true});fs.writeFileSync(path.join(dir,'plan.json'),JSON.stringify(p,null,2));assert.equal(p.exportRows.length,11);const r=build({...source,market,variants:source.variants.map(()=>({price:19.95,quantity:0,length:31}))}).exportRows[1];assert.equal(r[market==='CA'?'FZ':'GD'],19.95);assert.equal(r[market==='CA'?'FV':'FZ'],0);assert.equal(r[market==='CA'?'HA':'HE'],market==='CA'?'Centimeters':'Centimetres');}assert.throws(()=>build({...source,market:'US'}));console.log('UK/CA mappings passed');
